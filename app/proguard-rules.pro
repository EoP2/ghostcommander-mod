# Add project specific ProGuard rules here.
#
# app/build.gradle currently sets `minifyEnabled false` for the release
# build type, so this file is not actively applied yet. It is kept here
# (empty) only because build.gradle's proguardFiles(...) call references
# it — without this file present, that reference would point at nothing.
#
# If you later enable minifyEnabled/shrinkResources for release builds,
# add your keep rules here.
