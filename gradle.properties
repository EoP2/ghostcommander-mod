# Project-wide Gradle settings.

# Increase build daemon heap and force UTF-8 (this project ships strings.xml
# for ~25 languages, so a consistent encoding matters).
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Speed up local and CI builds.
org.gradle.parallel=true
org.gradle.caching=true
